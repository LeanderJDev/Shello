# Shello

Rechnernetze Projekt - Chat App

## Development

### Backend

Braucht API Key, um auf die Datenbank zuzugreifen.
Es gibt eine Beispieldatei `.env.example`, die in `.env` kopiert und mit den eigenen Daten gefüllt werden muss.

Danach kann der Server mit folgendem Befehl gestartet werden:

```bash
python -m backend.server
```
